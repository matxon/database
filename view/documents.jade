extends layout


block content
  section
    h1 Документы 
      small ATYRAUGEOCONTROL
    ol.breadcrumb
      li
        a(href='/docs') Database
      li.active Документы
      if username
        li 
          em #{username}

  section
    .row
      .col-md-12
        .box.box-solid
          .box-header
          .box-body
            .table-responsive
              table.table.table-bordered.table-hover(style="font-size:14px")
                thead
                  tr
                    th #
                    th Құжаттар топтамасы
                    th Сипаттамасы
                    th Құжаттың иесі
                tbody
                  each val in title
                    tr
                      td #{val.document_id}
                      td #{val.document_folder}
                      td #{val.description}
                      td #{val.company_name}
            button#opener.btn.btn-default(type="submit") Add Document
  include ./includes/dialog-document.jade

block scripts
  script(src="javascripts/document.js")
  script.
    var a = 3;
    console.log(a);
